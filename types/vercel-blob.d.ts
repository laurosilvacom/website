// Intentionally empty.
// This file previously declared a local module shim for '@vercel/blob', but that
// overrides the official package typings and can hide real exports (e.g. head()).
// Keep it as a module so TS stops treating it as global script.
export {}
